hisotria de usuario semana 4 me solicita:
- un index con formulario para agregar datos como productos o tareas puesto en una lista ul para renderizar elementos y boton para sincronizar con la api
- archivo app.js que contiene la logica de interaccion, persistencia y comunicacion con el servidor

-solicitar al usuario desde el dom o prompt dependiendo si es tarea o productos: producto, precio o descripcion. Validando los campos para evitar datos vacios o invalidos. (trycatch)
-muestra mensajes dinamicos en caso de exito o error en consola o dom

- permite agregar elementos al dom, creal elementos li dinamicamente agregando boton eliminar, tambien implementar la funcion para borrar un elemento al hacer clic
-uso de appendchild y removechild

- guardar los datos agregados en el navegador para mantenerlos entre sesiones y arreglo global para almacenar datos con localstorage

-usar jsonserver con sus metodos.

-revisar que todo funcione y documentar. 