let task =[];
let num = 0;

const increment =()=>{
    num++
    console.log(`tarea N°${num}`);
    contador.innerHTML = `<h2>¡Agregaste la tarea N°${num}!</h2>;`;
    task.push(title)
    console.log();
}