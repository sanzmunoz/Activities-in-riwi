hoisting:
    Comportamiento en JS por el cual las declaraciones de las variables las eleva a un ambito global

Scope:
    Alcance que tienen las variables dependiendo de donde esten ubicadas en el bloque de codigo

Clousures:
    Funcion que "recuerda" el scope en el que fue creada, incluso cuando se ejecuta fuera del scope, permite que la funcion acceda a variables     --funcion que retorna otra funcion

Ejecucion sincronica y asincronica:

callBack:
    funcion que se pasa como argumento a otra funcion

    TERMINAR DE LEER CLASE 3
    PRIORIDAD ENTERDER ASYNG AWAIT Y LUEGO IMPLEMENTAR TRY-CATCH